version: "3.9"

services:

  db2:
    build: ./Db
    ports:
      - 1433:1433
    healthcheck:
      test: ["CMD", "nc", "-z", "localhost", "1433"]
      interval: 20s
      timeout: 10s
      retries: 10
    networks:
      - local

networks:
  local:
    name: local
    driver: bridge